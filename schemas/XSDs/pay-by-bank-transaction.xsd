<?xml version="1.0" encoding="UTF-8"?>
<!--W3C Schema generated by XMLSpy v2009 sp1 (http://www.altova.com) -->
<xs:schema xmlns="http://ws.plimus.com" xmlns:xs="http://www.w3.org/2001/XMLSchema"
	xmlns:jaxb="http://java.sun.com/xml/ns/jaxb" jaxb:version="2.0"
	targetNamespace="http://ws.plimus.com">
    <!-- for iban -->
	<xs:include schemaLocation="local-bank-transfer-info.xsd" />
    <!-- for return-url, cancel-url -->
	<xs:include schemaLocation="url-common.xsd" />
    <!-- for iban-first/last-four -->
	<xs:include schemaLocation="sepa-direct-debit-info.xsd" />


	<xs:element name="pay-by-bank-transaction">
		<xs:complexType>
			<xs:sequence>
				<!-- for the request: -->
				<xs:element ref="return-url" minOccurs="0" maxOccurs="1" />
				<xs:element ref="cancel-url" minOccurs="0" maxOccurs="1" />
				<xs:element ref="iban" />

				<!-- for the response: -->
				<xs:element ref="pay-by-bank-url" minOccurs="0" maxOccurs="1" />
				<xs:element ref="iban-first-four" />
				<xs:element ref="iban-last-four" />
			</xs:sequence>
		</xs:complexType>
	</xs:element>

	<xs:element name="pay-by-bank-url">
		<xs:simpleType>
			<xs:restriction base="xs:string" />
		</xs:simpleType>
	</xs:element>

</xs:schema>
